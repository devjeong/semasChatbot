# 단계별 작업 기능 구현 작업일지

## 📋 요구사항 요약

### 핵심 목표
사용자의 요구사항을 단순히 LLM에 질문하는 것이 아니라, 작업을 세분화하여 체계적으로 관리하고 실행하는 워크플로우를 구현합니다.

### 주요 기능 요구사항
1. **작업목록 생성**: 사용자 요구사항을 기반으로 **Gemini API**를 통해 작업목록을 to-do 형식으로 생성
2. **작업목록 표시 및 저장**: 
   - 채팅창에 요약된 목록 표시
   - 상세내용은 로컬 PC 특정 경로에 `날짜_요구사항명.md` 파일로 저장
   - '진행하기', '취소하기' 버튼 표시
3. **취소 기능 (작업목록 단계)**:
   - '취소하기' 버튼 클릭 시 작업 세션 취소
   - 생성된 `.md` 파일 삭제
4. **작업별 순차 처리**: '진행하기' 클릭 시 작업별로 순서대로 처리
5. **작업별 프롬프트 생성**: 각 작업별로 **Gemini API**를 통해 자세한 프롬프트 생성
6. **프롬프트 승인 단계**:
   - 생성된 프롬프트를 사용자에게 제시
   - '진행', '취소' 버튼 표시
   - '취소' 클릭 시 이후 모든 작업목록 취소
7. **작업 실행**: 
   - '진행' 클릭 시 선택된 모델(Gemini/LM Studio)에 따라 API 호출
   - 결과를 받아와서 표시
8. **작업 완료 후 처리**:
   - 결과 표시 후 '완료' 또는 '취소' 버튼 제공
   - '완료' 클릭 시 다음 작업으로 진행
   - '취소' 클릭 시 모든 작업 취소

### 제약 조건
- 기존 ChatService 구조와의 호환성 유지
- Gemini API를 통한 작업목록 생성 및 프롬프트 생성
- 파일 시스템을 통한 작업 이력 관리
- 사용자 경험(UX) 최적화

---

## 🎯 작업 목록

### Phase 1: 데이터 모델 및 상태 관리 설계 ✅
- [x] **Task 1.1**: 작업(Task) 데이터 모델 설계 및 구현
- [x] **Task 1.2**: 작업 세션(TaskSession) 상태 관리 클래스 구현
- [x] **Task 1.3**: 작업 상태 열거형(TaskStatus) 정의

### Phase 2: Gemini API 통합 - 작업목록 생성 ✅
- [x] **Task 2.1**: 작업목록 생성용 Gemini API 호출 함수 구현
- [x] **Task 2.2**: Gemini 응답 파싱 및 Task 객체 리스트 변환 로직 구현
- [x] **Task 2.3**: 작업목록 생성 실패 시 에러 처리 및 재시도 로직

### Phase 3: 파일 시스템 통합 - 작업 이력 저장 ✅
- [x] **Task 3.1**: 작업 이력 저장 디렉토리 구조 설계 및 생성 로직
- [x] **Task 3.2**: `날짜_요구사항명.md` 파일 생성 및 상세 내용 저장 함수 구현
- [x] **Task 3.3**: 파일명 중복 처리 및 안전한 파일명 생성 유틸리티

### Phase 4: UI 컴포넌트 - 작업목록 표시 및 상호작용 ✅
- [x] **Task 4.1**: 채팅창에 작업목록 요약 표시 UI 컴포넌트 구현
- [x] **Task 4.2**: 작업 진행 여부 확인 다이얼로그/버튼 UI 구현
- [x] **Task 4.3**: 작업별 상태 표시 UI (진행중/완료/취소) 구현
- [x] **Task 4.4**: 작업별 취소 버튼 및 전체 취소 버튼 UI 구현

### Phase 5: 작업 실행 엔진 - 단계별 작업 처리 🔄
- [x] **Task 5.1**: 작업별 프롬프트 생성용 Gemini API 호출 함수 구현
- [x] **Task 5.2**: 작업 실행 상태 머신(State Machine) 구현
- [x] **Task 5.3**: 작업 순차 실행 로직 및 다음 작업으로 전환 로직 구현
- [ ] **Task 5.4**: 프롬프트 제시 후 '진행'/'취소' 버튼 UI 구현
- [ ] **Task 5.5**: 선택된 모델에 따른 API 호출 로직 구현 (Gemini/LM Studio)
- [ ] **Task 5.6**: 작업 결과 표시 후 '완료'/'취소' 버튼 UI 구현
- [ ] **Task 5.7**: 작업 완료 후 다음 작업 진행 또는 전체 취소 로직 구현

### Phase 6: 취소 기능 구현 🔄
- [x] **Task 6.1**: 개별 작업 취소 로직 구현
- [x] **Task 6.2**: 전체 작업 세션 취소 로직 구현
- [x] **Task 6.3**: 취소 시 상태 정리 및 리소스 해제 로직
- [ ] **Task 6.4**: 취소 시 .md 파일 삭제 기능 구현

### Phase 7: ChatService 통합 ✅
- [x] **Task 7.1**: ChatService에 작업 모드 감지 로직 추가
- [x] **Task 7.2**: 기존 sendChatRequestToLLM과 작업 모드 통합
- [x] **Task 7.3**: 작업 모드와 일반 채팅 모드 전환 로직

### Phase 8: 테스트 및 최적화 ⏳
- [ ] **Task 8.1**: 단위 테스트 작성 (Task 모델, 상태 관리)
- [ ] **Task 8.2**: 통합 테스트 작성 (Gemini API 통합, 파일 저장)
- [ ] **Task 8.3**: 성능 최적화 (API 호출 최소화, 비동기 처리)
- [ ] **Task 8.4**: 에러 처리 강화 및 사용자 피드백 개선

---

## 📝 작업별 상세 구현 계획

### Task 1.1: 작업(Task) 데이터 모델 설계 및 구현

#### 목표
작업 정보를 담는 데이터 클래스를 설계하고 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업 단위를 나타내는 데이터 클래스
 * @param id 작업 고유 식별자
 * @param title 작업 제목
 * @param description 작업 상세 설명
 * @param status 작업 상태 (PENDING, IN_PROGRESS, COMPLETED, CANCELLED)
 * @param prompt 작업 실행 시 사용할 프롬프트 (생성 전에는 null)
 * @param result 작업 실행 결과 (완료 후 저장)
 * @param order 작업 실행 순서
 */
data class Task(
    val id: String,
    val title: String,
    val description: String,
    var status: TaskStatus,
    var prompt: String? = null,
    var result: String? = null,
    val order: Int
)

enum class TaskStatus {
    PENDING,        // 대기 중
    IN_PROGRESS,    // 진행 중
    COMPLETED,      // 완료
    CANCELLED,      // 취소됨
    FAILED          // 실패
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/task/Task.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/TaskStatus.kt`

#### 테스트 계획
- Task 객체 생성 및 필드 검증
- TaskStatus 전환 로직 검증

---

### Task 1.2: 작업 세션(TaskSession) 상태 관리 클래스 구현

#### 목표
여러 작업을 그룹으로 관리하고 세션 상태를 추적하는 클래스를 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업 세션을 관리하는 클래스
 * 사용자 요구사항에 대한 전체 작업 세트를 관리합니다.
 */
class TaskSession(
    val id: String,
    val requirement: String,  // 사용자 요구사항
    val tasks: MutableList<Task>,
    val createdAt: Long = System.currentTimeMillis(),
    var status: SessionStatus = SessionStatus.CREATED
) {
    /**
     * 현재 진행 중인 작업 반환
     */
    fun getCurrentTask(): Task? {
        return tasks.firstOrNull { it.status == TaskStatus.IN_PROGRESS }
            ?: tasks.firstOrNull { it.status == TaskStatus.PENDING }
    }
    
    /**
     * 모든 작업 완료 여부 확인
     */
    fun isAllCompleted(): Boolean {
        return tasks.all { 
            it.status == TaskStatus.COMPLETED || 
            it.status == TaskStatus.CANCELLED 
        }
    }
    
    /**
     * 진행률 계산 (0.0 ~ 1.0)
     */
    fun getProgress(): Double {
        val completed = tasks.count { it.status == TaskStatus.COMPLETED }
        return if (tasks.isEmpty()) 0.0 else completed.toDouble() / tasks.size
    }
}

enum class SessionStatus {
    CREATED,        // 생성됨 (작업목록만 생성)
    APPROVED,       // 사용자 승인됨
    IN_PROGRESS,    // 진행 중
    COMPLETED,      // 완료
    CANCELLED       // 취소됨
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/task/TaskSession.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/SessionStatus.kt`

#### 테스트 계획
- TaskSession 생성 및 작업 추가
- 진행률 계산 로직 검증
- 현재 작업 조회 로직 검증

---

### Task 2.1: 작업목록 생성용 Gemini API 호출 함수 구현

#### 목표
사용자 요구사항을 받아 Gemini API를 통해 작업목록을 생성하는 함수를 구현합니다.

#### 구현 내용
```kotlin
/**
 * Gemini API를 통해 작업목록을 생성합니다.
 * @param requirement 사용자 요구사항
 * @return Task 리스트
 */
class TaskListGenerator(private val geminiClient: GeminiClient) {
    
    suspend fun generateTaskList(requirement: String): List<Task> {
        val prompt = buildTaskListPrompt(requirement)
        val response = geminiClient.sendChatRequest(prompt)
        return parseTaskListResponse(response)
    }
    
    private fun buildTaskListPrompt(requirement: String): String {
        return """
            사용자의 다음 요구사항을 분석하여 구체적이고 실행 가능한 작업 목록을 생성해주세요.
            
            요구사항: $requirement
            
            출력 형식은 반드시 다음 JSON 형식을 따라야 합니다:
            {
                "tasks": [
                    {
                        "id": "task_1",
                        "title": "작업 제목",
                        "description": "작업에 대한 상세 설명",
                        "order": 1
                    },
                    ...
                ]
            }
            
            각 작업은:
            - 독립적으로 실행 가능해야 합니다
            - 명확한 목표를 가져야 합니다
            - 순서가 중요하다면 order 필드로 순서를 명시해야 합니다
            - 최대 10개 이하로 제한합니다
        """.trimIndent()
    }
    
    private fun parseTaskListResponse(response: String): List<Task> {
        // JSON 파싱 로직
        // JSON 라이브러리 사용 (예: kotlinx.serialization 또는 Gson)
        // 파싱 실패 시 예외 처리
    }
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/task/TaskListGenerator.kt`

#### 의존성
- `GeminiClient` (기존 클래스 활용)
- JSON 파싱 라이브러리

#### 테스트 계획
- 정상적인 Gemini 응답 파싱 테스트
- 잘못된 형식의 응답 처리 테스트
- 네트워크 오류 처리 테스트

---

### Task 2.2: Gemini 응답 파싱 및 Task 객체 리스트 변환 로직 구현

#### 목표
Gemini API 응답을 안전하게 파싱하여 Task 객체 리스트로 변환합니다.

#### 구현 내용
```kotlin
/**
 * Gemini 응답을 파싱하여 Task 리스트로 변환
 */
private fun parseTaskListResponse(response: String): List<Task> {
    return try {
        // JSON 응답에서 tasks 배열 추출
        val jsonObject = Json.parseToJsonElement(response).jsonObject
        val tasksArray = jsonObject["tasks"]?.jsonArray ?: throw ParseException("tasks 필드가 없습니다")
        
        tasksArray.mapIndexed { index, taskJson ->
            val taskObj = taskJson.jsonObject
            Task(
                id = taskObj["id"]?.jsonPrimitive?.content ?: "task_${index + 1}",
                title = taskObj["title"]?.jsonPrimitive?.content ?: "작업 ${index + 1}",
                description = taskObj["description"]?.jsonPrimitive?.content ?: "",
                status = TaskStatus.PENDING,
                order = taskObj["order"]?.jsonPrimitive?.int ?: (index + 1)
            )
        }.sortedBy { it.order }
    } catch (e: Exception) {
        // 파싱 실패 시 기본 작업 생성
        listOf(
            Task(
                id = "task_1",
                title = "요구사항 분석 및 구현",
                description = requirement,
                status = TaskStatus.PENDING,
                order = 1
            )
        )
    }
}
```

#### 에러 처리
- JSON 파싱 실패 시 기본 작업 1개 생성
- 필수 필드 누락 시 기본값 사용
- 로깅을 통한 디버깅 정보 기록

---

### Task 3.1: 작업 이력 저장 디렉토리 구조 설계 및 생성 로직

#### 목표
작업 이력을 저장할 디렉토리 구조를 설계하고 생성 로직을 구현합니다.

#### 디렉토리 구조
```
{workspace}/.semasChatbot/
  └── task_history/
      └── YYYY-MM-DD/
          └── YYYY-MM-DD_HHmmss_요구사항명.md
```

#### 구현 내용
```kotlin
/**
 * 작업 이력 파일 관리 클래스
 */
class TaskHistoryManager(private val project: Project) {
    
    private val baseDir: File
        get() {
            val workspaceRoot = project.basePath ?: return File(System.getProperty("user.home"))
            val semasDir = File(workspaceRoot, ".semasChatbot")
            val historyDir = File(semasDir, "task_history")
            val dateDir = File(historyDir, getCurrentDateString())
            
            // 디렉토리 생성
            dateDir.mkdirs()
            return dateDir
        }
    
    /**
     * 작업 세션을 마크다운 파일로 저장
     */
    fun saveTaskSession(session: TaskSession): File {
        val fileName = generateFileName(session.requirement)
        val file = File(baseDir, fileName)
        
        val content = buildMarkdownContent(session)
        file.writeText(content, Charsets.UTF_8)
        
        return file
    }
    
    private fun generateFileName(requirement: String): String {
        val timestamp = SimpleDateFormat("yyyy-MM-dd_HHmmss", Locale.getDefault())
            .format(Date())
        val sanitizedRequirement = requirement
            .replace(Regex("[^가-힣a-zA-Z0-9\\s]"), "")
            .take(50)
            .replace(" ", "_")
        return "${timestamp}_${sanitizedRequirement}.md"
    }
    
    private fun getCurrentDateString(): String {
        return SimpleDateFormat("yyyy-MM-dd", Locale.getDefault())
            .format(Date())
    }
    
    private fun buildMarkdownContent(session: TaskSession): String {
        return buildString {
            appendLine("# 작업 세션: ${session.requirement}")
            appendLine()
            appendLine("**생성 시간**: ${formatDateTime(session.createdAt)}")
            appendLine("**상태**: ${session.status}")
            appendLine("**진행률**: ${(session.getProgress() * 100).toInt()}%")
            appendLine()
            appendLine("## 작업 목록")
            appendLine()
            session.tasks.forEachIndexed { index, task ->
                appendLine("### ${index + 1}. ${task.title}")
                appendLine()
                appendLine("**상태**: ${task.status}")
                appendLine("**설명**: ${task.description}")
                appendLine()
                if (task.prompt != null) {
                    appendLine("**프롬프트**:")
                    appendLine("```")
                    appendLine(task.prompt)
                    appendLine("```")
                    appendLine()
                }
                if (task.result != null) {
                    appendLine("**결과**:")
                    appendLine("```")
                    appendLine(task.result)
                    appendLine("```")
                    appendLine()
                }
                appendLine("---")
                appendLine()
            }
        }
    }
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/task/TaskHistoryManager.kt`

#### 테스트 계획
- 디렉토리 생성 로직 테스트
- 파일명 생성 및 중복 처리 테스트
- 마크다운 콘텐츠 생성 테스트

---

### Task 4.1: 채팅창에 작업목록 요약 표시 UI 컴포넌트 구현

#### 목표
생성된 작업목록을 채팅창에 요약 형태로 표시하는 UI 컴포넌트를 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업목록을 채팅창에 표시하는 UI 컴포넌트
 */
class TaskListPanel(
    private val session: TaskSession,
    private val onApprove: () -> Unit,
    private val onCancel: () -> Unit
) : JPanel() {
    
    init {
        layout = BoxLayout(this, BoxLayout.Y_AXIS)
        border = BorderFactory.createEmptyBorder(10, 10, 10, 10)
        
        // 헤더
        val headerLabel = JLabel("<html><b>📋 작업 목록이 생성되었습니다</b></html>")
        headerLabel.font = headerLabel.font.deriveFont(Font.BOLD, 14f)
        add(headerLabel)
        
        add(Box.createVerticalStrut(10))
        
        // 요구사항 표시
        val requirementLabel = JLabel("<html><b>요구사항:</b> ${session.requirement}</html>")
        requirementLabel.border = BorderFactory.createEmptyBorder(5, 0, 10, 0)
        add(requirementLabel)
        
        // 작업 목록 표시
        val taskListPanel = JPanel()
        taskListPanel.layout = BoxLayout(taskListPanel, BoxLayout.Y_AXIS)
        
        session.tasks.forEachIndexed { index, task ->
            val taskItem = createTaskItem(index + 1, task)
            taskListPanel.add(taskItem)
            taskListPanel.add(Box.createVerticalStrut(5))
        }
        
        val scrollPane = JScrollPane(taskListPanel)
        scrollPane.preferredSize = Dimension(600, 200)
        scrollPane.maximumSize = Dimension(600, 200)
        add(scrollPane)
        
        add(Box.createVerticalStrut(10))
        
        // 진행 여부 버튼
        val buttonPanel = JPanel(FlowLayout(FlowLayout.RIGHT))
        val approveButton = JButton("✅ 진행하기")
        approveButton.addActionListener { onApprove() }
        
        val cancelButton = JButton("❌ 취소")
        cancelButton.addActionListener { onCancel() }
        
        buttonPanel.add(approveButton)
        buttonPanel.add(cancelButton)
        add(buttonPanel)
    }
    
    private fun createTaskItem(number: Int, task: Task): JPanel {
        val panel = JPanel(BorderLayout())
        panel.border = BorderFactory.createCompoundBorder(
            BorderFactory.createLineBorder(Color.LIGHT_GRAY),
            BorderFactory.createEmptyBorder(5, 10, 5, 10)
        )
        
        val titleLabel = JLabel("<html><b>$number. ${task.title}</b></html>")
        val descLabel = JLabel("<html>${task.description.take(100)}${if (task.description.length > 100) "..." else ""}</html>")
        descLabel.foreground = Color.GRAY
        
        val textPanel = JPanel()
        textPanel.layout = BoxLayout(textPanel, BoxLayout.Y_AXIS)
        textPanel.add(titleLabel)
        textPanel.add(descLabel)
        
        panel.add(textPanel, BorderLayout.CENTER)
        
        return panel
    }
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/ui/TaskListPanel.kt`

#### 통합 위치
- `ChatService.sendMessage()` 메서드에 통합
- 작업목록 생성 시 특별한 메시지 타입으로 처리

---

### Task 5.1: 작업별 프롬프트 생성용 Gemini API 호출 함수 구현

#### 목표
각 작업에 대해 가장 적절한 프롬프트를 생성하는 함수를 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업별 프롬프트를 생성하는 클래스
 */
class TaskPromptGenerator(private val geminiClient: GeminiClient) {
    
    suspend fun generatePromptForTask(
        task: Task,
        requirement: String,
        previousTasks: List<Task>
    ): String {
        val prompt = buildPromptGenerationRequest(task, requirement, previousTasks)
        val response = geminiClient.sendChatRequest(prompt)
        return extractPrompt(response)
    }
    
    private fun buildPromptGenerationRequest(
        task: Task,
        requirement: String,
        previousTasks: List<Task>
    ): String {
        val previousContext = if (previousTasks.isNotEmpty()) {
            buildString {
                appendLine("이전에 완료된 작업들:")
                previousTasks.forEach { prevTask ->
                    appendLine("- ${prevTask.title}: ${prevTask.result?.take(200)}")
                }
            }
        } else {
            "이전 작업 없음"
        }
        
        return """
            다음 작업을 수행하기 위한 최적의 프롬프트를 생성해주세요.
            
            전체 요구사항: $requirement
            
            현재 작업:
            - 제목: ${task.title}
            - 설명: ${task.description}
            
            $previousContext
            
            프롬프트 요구사항:
            1. 작업을 정확하게 수행할 수 있어야 합니다
            2. 이전 작업의 결과를 참고할 수 있어야 합니다
            3. 구체적이고 실행 가능해야 합니다
            4. 코드 생성이 필요한 경우 적절한 언어와 프레임워크를 명시해야 합니다
            
            프롬프트만 출력해주세요 (설명 없이).
        """.trimIndent()
    }
    
    private fun extractPrompt(response: String): String {
        // 응답에서 프롬프트 추출
        // 필요시 정제 로직 추가
        return response.trim()
    }
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/task/TaskPromptGenerator.kt`

---

### Task 5.2: 작업 실행 상태 머신(State Machine) 구현

#### 목표
작업의 상태 전환을 관리하는 상태 머신을 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업 실행 상태 머신
 */
class TaskExecutionStateMachine(private val session: TaskSession) {
    
    private var currentTaskIndex: Int = 0
    
    /**
     * 다음 작업으로 진행
     */
    fun moveToNextTask(): Task? {
        val nextTask = session.tasks
            .filter { it.status == TaskStatus.PENDING }
            .minByOrNull { it.order }
        
        if (nextTask != null) {
            nextTask.status = TaskStatus.IN_PROGRESS
        }
        
        return nextTask
    }
    
    /**
     * 현재 작업 완료 처리
     */
    fun completeCurrentTask(result: String) {
        val currentTask = getCurrentTask()
        currentTask?.let {
            it.status = TaskStatus.COMPLETED
            it.result = result
        }
    }
    
    /**
     * 특정 작업 취소
     */
    fun cancelTask(taskId: String) {
        session.tasks.find { it.id == taskId }?.let {
            it.status = TaskStatus.CANCELLED
        }
    }
    
    /**
     * 전체 세션 취소
     */
    fun cancelSession() {
        session.status = SessionStatus.CANCELLED
        session.tasks.forEach { task ->
            if (task.status == TaskStatus.PENDING || task.status == TaskStatus.IN_PROGRESS) {
                task.status = TaskStatus.CANCELLED
            }
        }
    }
    
    /**
     * 현재 작업 반환
     */
    fun getCurrentTask(): Task? {
        return session.getCurrentTask()
    }
    
    /**
     * 모든 작업 완료 여부 확인
     */
    fun isAllCompleted(): Boolean {
        return session.isAllCompleted()
    }
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/task/TaskExecutionStateMachine.kt`

---

### Task 6.1: 개별 작업 취소 로직 구현

#### 목표
사용자가 특정 작업을 취소할 수 있는 기능을 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업 취소 처리
 */
fun cancelTask(taskId: String) {
    val task = session.tasks.find { it.id == taskId }
    if (task == null) {
        sendMessage("❌ 작업을 찾을 수 없습니다.", isUser = false)
        return
    }
    
    when (task.status) {
        TaskStatus.COMPLETED -> {
            sendMessage("⚠️ 이미 완료된 작업입니다. 취소할 수 없습니다.", isUser = false)
        }
        TaskStatus.CANCELLED -> {
            sendMessage("⚠️ 이미 취소된 작업입니다.", isUser = false)
        }
        TaskStatus.IN_PROGRESS -> {
            // 진행 중인 작업 취소
            stateMachine.cancelTask(taskId)
            sendMessage("✅ 작업 '${task.title}'이(가) 취소되었습니다.", isUser = false)
            
            // 다음 작업으로 진행할지 확인
            val nextTask = stateMachine.moveToNextTask()
            if (nextTask != null) {
                sendMessage("다음 작업으로 진행합니다: ${nextTask.title}", isUser = false)
                executeTask(nextTask)
            } else {
                sendMessage("모든 작업이 완료되거나 취소되었습니다.", isUser = false)
            }
        }
        TaskStatus.PENDING -> {
            stateMachine.cancelTask(taskId)
            sendMessage("✅ 작업 '${task.title}'이(가) 취소되었습니다.", isUser = false)
        }
        else -> {}
    }
    
    // UI 업데이트
    updateTaskListUI()
}
```

---

### Task 6.4: 취소 시 .md 파일 삭제 기능 구현

#### 목표
작업 세션 취소 시 생성된 .md 파일을 삭제하는 기능을 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업 이력 파일 삭제
 * TaskHistoryManager에 추가할 메서드
 */
fun deleteTaskSessionFile(file: File): Boolean {
    return try {
        if (file.exists() && file.isFile) {
            val deleted = file.delete()
            if (deleted) {
                println("[TaskHistoryManager] 파일 삭제 완료: ${file.absolutePath}")
            } else {
                println("[TaskHistoryManager] 파일 삭제 실패: ${file.absolutePath}")
            }
            deleted
        } else {
            println("[TaskHistoryManager] 파일이 존재하지 않습니다: ${file.absolutePath}")
            false
        }
    } catch (e: Exception) {
        println("[TaskHistoryManager] 파일 삭제 중 오류 발생: ${e.message}")
        e.printStackTrace()
        false
    }
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/task/TaskHistoryManager.kt` (수정)

#### 통합 위치
- `ChatService.cancelTaskSession()` 메서드에서 호출
- 작업 세션 취소 시 저장된 파일 삭제

---

### Task 5.4: 프롬프트 제시 후 '진행'/'취소' 버튼 UI 구현

#### 목표
생성된 프롬프트를 사용자에게 제시하고, '진행' 또는 '취소' 버튼을 제공하는 UI 컴포넌트를 구현합니다.

#### 구현 내용
```kotlin
/**
 * 프롬프트 승인 UI 컴포넌트
 */
class PromptApprovalPanel(
    private val task: Task,
    private val prompt: String,
    private val onApprove: () -> Unit,
    private val onCancel: () -> Unit
) : JPanel() {
    init {
        layout = BoxLayout(this, BoxLayout.Y_AXIS)
        border = CompoundBorder(
            LineBorder(Color(52, 152, 219), 2, true),
            EmptyBorder(15, 15, 15, 15)
        )
        
        // 작업 정보
        val taskLabel = JLabel("<html><b>작업: ${task.title}</b></html>")
        add(taskLabel)
        add(Box.createVerticalStrut(10))
        
        // 프롬프트 내용 표시
        val promptArea = JTextArea(prompt)
        promptArea.isEditable = false
        promptArea.lineWrap = true
        promptArea.wrapStyleWord = true
        val scrollPane = JScrollPane(promptArea)
        scrollPane.preferredSize = Dimension(600, 200)
        add(scrollPane)
        
        add(Box.createVerticalStrut(10))
        
        // 버튼 패널
        val buttonPanel = JPanel(FlowLayout(FlowLayout.RIGHT, 10, 0))
        val approveButton = JButton("✅ 진행")
        approveButton.addActionListener { onApprove() }
        
        val cancelButton = JButton("❌ 취소")
        cancelButton.addActionListener { onCancel() }
        
        buttonPanel.add(approveButton)
        buttonPanel.add(cancelButton)
        add(buttonPanel)
    }
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/ui/PromptApprovalPanel.kt`

---

### Task 5.5: 선택된 모델에 따른 API 호출 로직 구현

#### 목표
현재 선택된 모델(Gemini 또는 LM Studio)에 따라 적절한 API를 호출하여 작업을 실행합니다.

#### 구현 내용
```kotlin
/**
 * 선택된 모델에 따라 작업 실행
 */
private fun executeTaskWithSelectedModel(task: Task, prompt: String) {
    val modelId = getSelectedModel()
    val systemMessage = "" // 필요시 시스템 메시지 추가
    
    ApplicationManager.getApplication().executeOnPooledThread {
        try {
            val result = if (isGeminiModel(modelId)) {
                // Gemini API 호출
                val actualModelId = modelId.removePrefix("💎 ").trim()
                geminiClient.sendChatRequest(
                    userMessage = prompt,
                    systemMessage = systemMessage,
                    modelId = actualModelId
                ) ?: "오류: Gemini API 응답이 null입니다."
            } else {
                // LM Studio API 호출
                apiClient.sendChatRequest(
                    userMessage = prompt,
                    systemMessage = systemMessage,
                    modelId = modelId
                ) ?: "오류: LM Studio API 응답이 null입니다."
            }
            
            ApplicationManager.getApplication().invokeLater {
                task.result = result
                displayTaskResult(task, result)
            }
        } catch (e: Exception) {
            ApplicationManager.getApplication().invokeLater {
                task.status = TaskStatus.FAILED
                task.result = "오류: ${e.message}"
                sendMessage("❌ 작업 실행 중 오류 발생: ${e.message}", isUser = false)
            }
        }
    }
}
```

#### 통합 위치
- `ChatService.executeNextTask()` 메서드에서 호출
- 프롬프트 승인 후 실행

---

### Task 5.6: 작업 결과 표시 후 '완료'/'취소' 버튼 UI 구현

#### 목표
작업 실행 결과를 표시하고, '완료' 또는 '취소' 버튼을 제공하는 UI 컴포넌트를 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업 결과 표시 UI 컴포넌트
 */
class TaskResultPanel(
    private val task: Task,
    private val result: String,
    private val onComplete: () -> Unit,
    private val onCancel: () -> Unit
) : JPanel() {
    init {
        layout = BoxLayout(this, BoxLayout.Y_AXIS)
        border = CompoundBorder(
            LineBorder(Color(46, 204, 113), 2, true),
            EmptyBorder(15, 15, 15, 15)
        )
        
        // 작업 정보
        val taskLabel = JLabel("<html><b>✅ 작업 완료: ${task.title}</b></html>")
        add(taskLabel)
        add(Box.createVerticalStrut(10))
        
        // 결과 내용 표시
        val resultArea = JTextArea(result)
        resultArea.isEditable = false
        resultArea.lineWrap = true
        resultArea.wrapStyleWord = true
        val scrollPane = JScrollPane(resultArea)
        scrollPane.preferredSize = Dimension(600, 300)
        add(scrollPane)
        
        add(Box.createVerticalStrut(10))
        
        // 버튼 패널
        val buttonPanel = JPanel(FlowLayout(FlowLayout.RIGHT, 10, 0))
        val completeButton = JButton("✅ 완료")
        completeButton.addActionListener { onComplete() }
        
        val cancelButton = JButton("❌ 취소")
        cancelButton.addActionListener { onCancel() }
        
        buttonPanel.add(completeButton)
        buttonPanel.add(cancelButton)
        add(buttonPanel)
    }
}
```

#### 파일 위치
- `src/main/kotlin/org/dev/semaschatbot/ui/TaskResultPanel.kt`

---

### Task 5.7: 작업 완료 후 다음 작업 진행 또는 전체 취소 로직 구현

#### 목표
작업 완료 후 사용자 선택에 따라 다음 작업으로 진행하거나 전체 작업을 취소하는 로직을 구현합니다.

#### 구현 내용
```kotlin
/**
 * 작업 완료 처리
 */
private fun handleTaskComplete() {
    val stateMachine = taskStateMachine ?: return
    val session = currentTaskSession ?: return
    
    // 현재 작업 완료 처리
    val currentTask = stateMachine.getCurrentTask()
    currentTask?.let {
        stateMachine.completeCurrentTask(it.result ?: "")
    }
    
    // .md 파일 업데이트
    updateTaskHistoryFile()
    
    // 다음 작업 확인
    if (stateMachine.isAllCompleted()) {
        sendMessage("✅ 모든 작업이 완료되었습니다!", isUser = false)
        session.status = SessionStatus.COMPLETED
        updateTaskHistoryFile()
    } else {
        // 다음 작업으로 진행
        executeNextTask()
    }
}

/**
 * 작업 취소 처리 (결과 단계에서)
 */
private fun handleTaskCancelFromResult() {
    cancelTaskSession()
}
```

#### 통합 위치
- `TaskResultPanel`의 콜백에서 호출
- '완료' 클릭 시 `handleTaskComplete()` 호출
- '취소' 클릭 시 `handleTaskCancelFromResult()` 호출

---

### Task 7.1: ChatService에 작업 모드 감지 로직 추가

#### 목표
사용자 입력이 작업 요청인지 일반 채팅인지 감지하는 로직을 추가합니다.

#### 구현 내용
```kotlin
/**
 * 작업 모드 감지
 */
private fun isTaskModeRequest(userInput: String): Boolean {
    val taskKeywords = listOf(
        "작업 목록", "작업목록", "할 일", "todo", "task list",
        "단계별", "순서대로", "단계로", "단계별로"
    )
    
    val input = userInput.lowercase()
    return taskKeywords.any { input.contains(it) } ||
           (input.contains("구현") && input.length > 50) || // 긴 구현 요청
           (input.contains("만들") && input.contains("기능"))
}

/**
 * 작업 모드 진입
 */
private fun enterTaskMode(requirement: String) {
    // 작업목록 생성 시작
    sendMessage("📋 작업 목록을 생성하는 중입니다...", isUser = false)
    
    // 비동기로 작업목록 생성
    ApplicationManager.getApplication().executeOnPooledThread {
        try {
            val taskListGenerator = TaskListGenerator(geminiClient)
            val tasks = taskListGenerator.generateTaskList(requirement)
            
            val session = TaskSession(
                id = UUID.randomUUID().toString(),
                requirement = requirement,
                tasks = tasks.toMutableList()
            )
            
            // 파일 저장
            val historyManager = TaskHistoryManager(project)
            val savedFile = historyManager.saveTaskSession(session)
            
            // UI 업데이트 (EDT 스레드에서)
            ApplicationManager.getApplication().invokeLater {
                displayTaskList(session, savedFile)
            }
        } catch (e: Exception) {
            ApplicationManager.getApplication().invokeLater {
                sendMessage("❌ 작업 목록 생성 중 오류가 발생했습니다: ${e.message}", isUser = false)
            }
        }
    }
}
```

---

## 🔄 전체 워크플로우

### 1단계: 작업목록 생성
```
사용자 입력 → 작업 모드 감지 → Gemini API 호출 → 작업목록 생성 → 파일 저장 → UI 표시
```

### 2단계: 작업목록 승인/취소
```
작업목록 표시 → '진행하기' 또는 '취소하기' 버튼 표시
├─ '취소하기' 클릭 → 작업 세션 취소 → .md 파일 삭제 → 종료
└─ '진행하기' 클릭 → 다음 단계로 진행
```

### 3단계: 작업별 프롬프트 생성 및 승인
```
작업 선택 → Gemini API로 프롬프트 생성 → 프롬프트 제시 → '진행' 또는 '취소' 버튼 표시
├─ '취소' 클릭 → 이후 모든 작업목록 취소 → .md 파일 삭제 → 종료
└─ '진행' 클릭 → 다음 단계로 진행
```

### 4단계: 작업 실행
```
선택된 모델 확인 → API 호출 (Gemini 또는 LM Studio) → 결과 수신 → 결과 표시
```

### 5단계: 작업 완료 후 처리
```
결과 표시 → '완료' 또는 '취소' 버튼 표시
├─ '취소' 클릭 → 모든 작업 취소 → .md 파일 삭제 → 종료
└─ '완료' 클릭 → 다음 작업으로 진행 (3단계로 돌아감)
```

### 6단계: 모든 작업 완료
```
모든 작업 완료 → 최종 .md 파일 업데이트 → 완료 메시지 표시
```

---

## 🧪 테스트 계획

### 단위 테스트
1. **Task 모델 테스트**
   - Task 객체 생성 및 필드 검증
   - TaskStatus 전환 검증

2. **TaskSession 테스트**
   - 진행률 계산 정확성
   - 현재 작업 조회 로직

3. **파싱 로직 테스트**
   - 정상 JSON 응답 파싱
   - 잘못된 형식 처리
   - 필수 필드 누락 처리

### 통합 테스트
1. **Gemini API 통합 테스트**
   - 작업목록 생성 API 호출
   - 프롬프트 생성 API 호출
   - 네트워크 오류 처리

2. **파일 시스템 통합 테스트**
   - 파일 생성 및 저장
   - 파일명 중복 처리
   - 마크다운 형식 검증

3. **UI 통합 테스트**
   - 작업목록 표시
   - 버튼 클릭 이벤트
   - 상태 업데이트 반영

---

## ⚡ 성능 최적화 고려사항

### 1. API 호출 최적화
- 작업목록 생성: 1회 호출
- 프롬프트 생성: 작업당 1회 호출
- 캐싱 전략: 동일 요구사항에 대한 재사용 고려

### 2. 비동기 처리
- 모든 Gemini API 호출은 백그라운드 스레드에서 실행
- UI 업데이트는 EDT 스레드에서만 수행
- 진행 상황 표시를 통한 사용자 경험 개선

### 3. 메모리 관리
- 완료된 작업 결과는 파일에 저장 후 메모리에서 제거 고려
- 대용량 응답 처리 시 스트리밍 방식 활용

### 4. 에러 복구
- 네트워크 오류 시 재시도 로직
- 부분 실패 시 나머지 작업 계속 진행 옵션
- 사용자에게 명확한 에러 메시지 제공

---

## 📊 구현 우선순위

### High Priority (Phase 1-3)
- 데이터 모델 및 상태 관리
- Gemini API 통합
- 파일 시스템 통합

### Medium Priority (Phase 4-5)
- UI 컴포넌트 구현
- 작업 실행 엔진

### Low Priority (Phase 6-8)
- 취소 기능
- ChatService 통합
- 테스트 및 최적화

---

## 📅 예상 작업 시간

- **Phase 1**: 2-3시간
- **Phase 2**: 3-4시간
- **Phase 3**: 2-3시간
- **Phase 4**: 4-5시간
- **Phase 5**: 4-5시간
- **Phase 6**: 2-3시간
- **Phase 7**: 3-4시간
- **Phase 8**: 4-6시간

**총 예상 시간**: 24-33시간

---

## 🔍 리스크 및 대응 방안

### 리스크 1: Gemini API 응답 형식 불일치
- **대응**: 파싱 실패 시 기본 작업 생성으로 폴백
- **검증**: 다양한 형식의 응답에 대한 테스트 케이스 작성

### 리스크 2: 파일 시스템 권한 문제
- **대응**: 디렉토리 생성 실패 시 사용자 홈 디렉토리 사용
- **검증**: 권한 없는 디렉토리에 대한 테스트

### 리스크 3: UI 성능 저하 (많은 작업)
- **대응**: 가상화된 리스트 뷰 사용 또는 페이지네이션
- **검증**: 100개 이상의 작업에 대한 성능 테스트

---

## 📝 다음 단계

1. **Phase 1 구현 시작**: 데이터 모델 및 상태 관리 클래스 구현
2. **Gemini API 테스트**: 작업목록 생성 API 호출 테스트
3. **프로토타입 개발**: 최소 기능으로 전체 워크플로우 검증
4. **점진적 개선**: 사용자 피드백 반영 및 기능 확장

---

## 📚 참고 자료

- 기존 `ChatService.kt` 구조 분석 완료
- `GeminiClient.kt` API 호출 방식 확인 완료
- WORKLOG 디렉토리 구조 및 파일 명명 규칙 확인 완료

---

**작성일**: 2024-01-XX
**작성자**: AI Assistant
**버전**: 1.0

---

## ✅ 구현 완료 내역 (2024-01-XX)

### Phase 1-7 구현 완료
모든 핵심 기능이 구현되었습니다:

1. **데이터 모델**: Task, TaskSession, TaskStatus, SessionStatus 구현 완료
2. **Gemini API 통합**: TaskListGenerator, TaskPromptGenerator 구현 완료
3. **파일 시스템**: TaskHistoryManager 구현 완료
4. **UI 컴포넌트**: TaskListPanel, TaskStatusPanel 구현 완료
5. **작업 실행 엔진**: TaskExecutionStateMachine 구현 완료
6. **ChatService 통합**: 작업 모드 감지 및 실행 로직 통합 완료

### 생성된 파일 목록
- `src/main/kotlin/org/dev/semaschatbot/task/Task.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/TaskStatus.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/TaskSession.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/SessionStatus.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/TaskListGenerator.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/TaskPromptGenerator.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/TaskExecutionStateMachine.kt`
- `src/main/kotlin/org/dev/semaschatbot/task/TaskHistoryManager.kt`
- `src/main/kotlin/org/dev/semaschatbot/ui/TaskListPanel.kt`
- `src/main/kotlin/org/dev/semaschatbot/ui/TaskStatusPanel.kt`
- `src/main/kotlin/org/dev/semaschatbot/ChatService.kt` (수정)

### 추가 구현 필요 사항

#### Phase 5 추가 작업
- **Task 5.4**: 프롬프트 제시 UI 컴포넌트 구현
  - 프롬프트 내용 표시
  - '진행', '취소' 버튼 추가
  - 취소 시 전체 작업 취소 로직 연동

- **Task 5.5**: 모델 선택에 따른 API 호출 구현
  - 현재 선택된 모델 확인 (Gemini 또는 LM Studio)
  - Gemini 모델: `geminiClient.sendChatRequest()` 사용
  - LM Studio 모델: `apiClient.sendChatRequest()` 사용
  - 결과를 Task.result에 저장

- **Task 5.6**: 작업 결과 표시 UI 컴포넌트 구현
  - 작업 결과 내용 표시
  - '완료', '취소' 버튼 추가
  - 완료 시 다음 작업 진행, 취소 시 전체 작업 취소

- **Task 5.7**: 작업 완료 후 처리 로직 구현
  - '완료' 클릭: 다음 작업으로 진행 (프롬프트 생성 단계로)
  - '취소' 클릭: 전체 작업 취소 및 .md 파일 삭제

#### Phase 6 추가 작업
- **Task 6.4**: 취소 시 .md 파일 삭제 기능
  - `TaskHistoryManager`에 파일 삭제 메서드 추가
  - 작업 세션 취소 시 저장된 파일 삭제
  - 파일 삭제 실패 시 에러 처리

### 다음 단계
1. **Phase 5 추가 구현**: 프롬프트 승인, 작업 실행, 결과 처리 UI 및 로직 구현
2. **Phase 6 추가 구현**: 취소 시 .md 파일 삭제 기능 구현
3. **Phase 8**: 테스트 및 최적화 작업 진행
4. **통합 테스트**: 전체 워크플로우 검증

